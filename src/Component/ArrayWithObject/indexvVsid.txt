✅ CORRECT WAY — using id (RECOMMENDED)
State + Add Student
function handleAddStudent() {
  const newStudent = {
    id: crypto.randomUUID(),   // stable unique identity
    name,
    year,
    course,
    currentYear: new Date().getFullYear(),
  };

  setStudents(prev => [...prev, newStudent]);
}
Render
<ul>
  {students.map(student => (
    <li key={student.id}>
      Name: {student.name} | DOB: {student.year} | Course: {student.course} | Year: {student.currentYear}
    </li>
  ))}
</ul>
Why this is correct

id never changes

React can match same item across renders

Safe for add / delete / edit

No DOM reuse bugs

❌ NOT RECOMMENDED — using index
<ul>
  {students.map((student, index) => (
    <li key={index}>
      Name: {student.name} | DOB: {student.year} | Course: {student.course} | Year: {student.currentYear}
    </li>
  ))}
</ul>

This runs, but it is logically wrong for your app.

🔥 WHY index BREAKS YOUR APP (REAL EXAMPLE)
Initial list
students = [
  { id: "a", name: "Ram" },   // index 0
  { id: "b", name: "Shyam" }, // index 1
  { id: "c", name: "Amit" }   // index 2
];

Keys if using index:

0, 1, 2
Remove middle student (Shyam)
students = [
  { id: "a", name: "Ram" },   // index 0
  { id: "c", name: "Amit" }   // index 1 ❌
];
React thinks:

Key 1 already exists

Reuse DOM from Shyam

Now DOM shows wrong data

💥 This causes:

Wrong student shown

Broken inputs

Bugs during edit/delete

🧠 WHY id FIXES THIS

With id keys:

a, b, c  →  a, c

React clearly sees:

b removed

a and c unchanged

Correct DOM updates

📌 SIMPLE RULE (MEMORIZE)
Situation	Use
Static list	index OK
Dynamic list (add/remove/edit)	❌ index
Your student list	✅ id
🏁 FINAL VERDICT

✔ Your app must use id
❌ index will create future bugs
✔ You already implemented the professional solution